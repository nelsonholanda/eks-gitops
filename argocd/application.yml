apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: devopsnanuvem
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/nelsonholanda/eks-gitops.git
    targetRevision: HEAD
    path: .
  destination:
    server: https://kubernetes.default.svc
    namespace: default
  syncPolicy:
    automated:
      selfHeal: true